bundle:
  name: nyc-mobility-pipeline

include:
  - resources/*.yml

targets:
  dev:
    mode: development

  prod:
    mode: production
    workspace:
      profile: razzelle.borerosa@ftwfoundation.org
      root_path: /Workspace/Users/razzelle.boreros@ftwfoundation.org/.bundle/${bundle.name}/${bundle.target}
    
