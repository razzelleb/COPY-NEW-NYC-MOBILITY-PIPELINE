bundle:
  name: nyc-mobility-pipeline

include:
  - resources/*.yml

targets:
  dev:
    mode: development

  prod:
    mode: production
    workspace:
      profile: sara.celadina@ftwfoundation.org
      root_path: /Workspace/Users/sara.celadina@ftwfoundation.org/.bundle/${bundle.name}/${bundle.target}
    
